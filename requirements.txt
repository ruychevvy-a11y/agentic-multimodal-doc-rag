# 检索 / 作答链路
dashscope>=1.27.2          # 通义百炼：嵌入、重排、视觉作答
pymilvus>=3.0.1            # 向量库客户端
numpy>=2.3

# 数据集构建与解析
pymupdf>=1.28.2            # PDF 渲染页图 + 取文本层
paddleocr>=3.7.0           # 扫描页 OCR（需要 paddlepaddle 或 paddlepaddle-gpu）
pillow>=12.0
datasets>=5.0.1            # 从 HuggingFace 下载 MMLongBench-Doc

# demo
fastapi>=0.141
uvicorn>=0.52
streamlit>=1.63            # 用到 st.container(horizontal=...) 等新接口
pydantic>=2.13

python-dotenv>=1.2
requests>=2.34
